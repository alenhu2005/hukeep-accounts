{
  "name": "ledger-app",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "icons:flatten": "node scripts/flatten-app-icons.mjs",
    "icons:prepare": "node scripts/prepare-app-icons.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "vitest": "^3.0.5"
  }
}
